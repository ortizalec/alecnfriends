version: '3.8'

services:
  backend:
    build: ./backend
    container_name: alecnfriends-backend
    restart: unless-stopped
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - DATABASE_PATH=/app/data/alecnfriends.db
    volumes:
      - backend-data:/app/data
    ports:
      - "8080:8080"
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build: ./frontend
    container_name: alecnfriends-frontend
    restart: unless-stopped
    volumes:
      - frontend-dist:/srv
    networks:
      - app-network

  caddy:
    image: caddy:2-alpine
    container_name: alecnfriends-caddy
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./Caddyfile.dev:/etc/caddy/Caddyfile
      - frontend-dist:/srv:ro
    depends_on:
      - frontend
      - backend
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  backend-data:
  frontend-dist:
